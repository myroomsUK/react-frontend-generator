import { AUTH_ENTRYPOINT } from "./entrypoint";
export const routes = {
    authentication_token: AUTH_ENTRYPOINT + "/authentication_token",
    property_image_multiple_upload: "property_images/multiple-upload",
};
